---
unit: v0.2.4
stage: DESIGN
lifecycle: LIVE
updated: 2026-09-15
---

# Unit v0.2.4 — Design delta: management CSV sample (`leadhs probe download-csv-sample`)

## Abstract

Design for unit v0.2.4 (strategy `10_STRATEGY/v0.2.4.md`, D36). One
new probe command draws, per registry, 100 random in-scope product
items with all available data fields as CSV for management review,
plus a generated manifest answering three questions per registry:
what fields are returned per product item, whether
cross-identification is possible, which identifier columns exist.
Development is one module + one Make target + tests; no schema change
— the only migration is 0009, a lookup-seed INSERT (probe_mode
`csv_sample` + the two probe_metric codes; the i4 vocabulary
extension path 0003/0005/0006/0007 use), pinned by the t6 sync test.

## Governing references

- Strategy: `10_STRATEGY/v0.2.4.md` (D36); D31 (recon-only), D18
  (numbers from the DB), D20 (probe entities), D22 (report routing),
  D2 (seeded sampling).
- Design: MASTER/interfaces.md (i15 recon contract, i19 staging),
  MASTER/testing.md; units/v0.2.2.md (fu2 staging semantics — not
  extended here).

## Command surface

    leadhs probe download-csv-sample
        [--n 100]                  # sample size per registry
        [--seed 42]                # seeded draw; recorded in run + CSV
        [--source ID]...           # subset (default: AS-2 AS-3 ST-1 ST-3 ST-6 ST-7)
        [--out-dir data/report]    # D22 routing
        [--dry-run]                # plan requests; zero network

## Per-registry behavior

| Source | Behavior |
|---|---|
| AS-2 ECAT | One bulk GET of the pinned export_url → raw-store archive → shared CSV parse (`read_csv_rows`) → in-scope group filter (reuse `_in_scope_group`) → `random.Random(seed).sample(n)` → CSV (all export columns verbatim + provenance columns) |
| AS-3 Nordic Swan | Bounded export discovery (≤5 polite rate-limited GETs on the known search surface; CSV/Excel endpoint patterns). Success → ECAT path with criterion-096 filter. Failure → honest manifest record ("export mechanics unpinned — browser pass required") |
| ST-1 PCN | No fetch. Manifest record: PCN database authority-only (appointed bodies; poisoncentres.echa.europa.eu, accessed 2026-09-14) |
| ST-7 SE | No fetch. Manifest record: KemI Products Register secrecy-protected at product level; public = aggregates (KemI-stat) |
| ST-6 DK | No fetch. Manifest record: aggregates-only via embedded Power BI (v0.2.3 PHASE02 shape gate) |
| ST-3 SBS | No fetch. Manifest record: enterprise statistics (NACE C2030) — no product items |

**Per-source state machine** (review Issue 1 → decision 1A: the
manifest is ALWAYS written and tells the truth per source; the exit
code flags the failure):

    pending ──▶ delivered    (csv_sample_rows; exit contribution 0)
         ────▶ unavailable  (csv_sample_unavailable, expected → exit 0)
         ────▶ failed       (should-deliver only → exit 2)

## Module placement

- `src/leadhs/probe/csv_sample.py` — the command module: per-source
  handlers, the seeded draw, CSV writers, manifest renderer. Reuses
  `fetch.Fetcher`, `store.RawStore`, `_common.read_csv_rows` +
  `sniff_kind` + `_delimiter_of`, the AS `_in_scope_group` +
  `_EXPORT_EXPECTED` (imported, not duplicated).
- Run bookkeeping rides the engine's single write path (i3): engine
  `_insert_run` / `_insert_probe_run` / `_finish_run` / `persist`
  (kind=probe, mode=`csv_sample`); the draw seed lands in the
  existing `run.seed` column (0002) — no schema change.
- CLI wiring in `cli.py` under the `probe` group.
- No engine changes; no adapter changes; no staging writes (the
  samples are review artifacts, not measurement rows).

## Outputs (data/report/, one shared UTC ts per invocation)

- `csv-sample.<ts>.<source-id>.csv` — per delivering registry; all
  export columns verbatim + `source_id`, `run_key`, `retrieval_date`,
  `doc_hash`, `sample_seed`, `sample_index`.
- `csv-sample.manifest.<ts>.md` + `.csv` — per registry: delivered? ·
  rows · fields returned (column list) · identifier columns ·
  cross-identification candidates · reason + citation when no rows
  are possible.
- Unversioned "latest" copies refreshed (render-history policy).

## Provenance

- Raw export archived `data/raw/AS-2/<sha256>.csv` (R2); every CSV
  row carries source_id, run_key, retrieval_date, doc_hash, seed.
- probe_run + findings per source: `csv_sample_rows` (numeric,
  delivered) / `csv_sample_unavailable` (text, expected-unavailable).
- Manifest reason strings cite the verified findings (v0.2.3
  PHASE02/03 records + the 2026-09-14 checks, URL + access date).
- Manifest constants live in the module + this method sheet (i4).

## Exit codes

- 0 — every source ended delivered or expected-unavailable.
- 2 — a should-deliver source failed (fetch error, column drift, 0
  in-scope rows after the filter). Consistent with the probe
  convention (i1); the census/recon/capability chains already
  tolerate exactly exit 2.
- Usage errors (`--n < 1`, unknown `--source`, uninitialized DB) —
  exit 1 per the i1 mapping (`main()` hardcodes UsageError → 1);
  never 2.

## Makefile

    sample-csv: guard-sample-csv ## management CSV samples per registry (GO=1)
    	$(LEADHS) probe download-csv-sample

(wired into `help`; `.PHONY` updated. The M1 milestone-gated `sample`
stub is a DIFFERENT thing and stays untouched — `sample-csv` is the
distinct v0.2.4 target name.)

## Testing (offline, `tests/test_csv_sample.py` + fixture-site paths)

- AS-2 happy path on the fixture export: seeded-draw determinism
  (same seed → same rows; different seed → different), column
  preservation, the 6 provenance columns present, pool clamping
  (n > pool → pool rows + manifest shortfall note).
- Dedupe before the draw (duplicate product+company+group triples →
  distinct items; pool size pre/post dedupe in the manifest).
- Manifest content for the four no-fetch sources (correct reasons
  with citations, zero network calls asserted via site hits).
- Nordic Swan discovery: HTML search surface → honest
  unavailable record, GETs bounded (≤5 asserted); a CSV response →
  the ECAT path with the criterion-096 filter.
- Manifest ALWAYS written (1A): mocked AS-2 failure → manifest
  exists with status=failed, exit 2; ST records still present.
- CLI: --dry-run plans, zero network, zero files; exit codes 0/0/2;
  unknown --source and --n 0 → exit 1; column drift →
  UnexpectedFormat → exit 2.
- Makefile: `sample-csv` present, guard wired (test_makefile.py
  pattern).
- Migration 0009: fresh apply + idempotency + the t6 seed sync
  (vocabulary 41).

## Review outcomes (2026-09-14 — CEO + ENG review, HOLD SCOPE)

Both reviews ran HOLD SCOPE (scope unchanged, S1–S4 in force).
Findings folded here:

- **1A (manifest semantics):** manifest always written with
  per-source delivered/unavailable/failed; exit 2 flags failure —
  zero silent failures (the report card exists and tells the truth).
- **Amendments:** in-scope pool < n → clamp + manifest shortfall
  note (stdlib `random.sample` would raise); draw from DISTINCT
  product items (product+company+group triple), pool size pre/post
  dedupe reported; `--n ≥ 1` and `--source` validated (exit 1);
  tests use an injected clock/ts (no flaky timestamps); AS-3
  discovery catches the named taxonomy only (no bare except); 0
  in-scope rows after the filter = failed (data-drift signal), not
  a silent empty CSV.
- **Migration correction:** strategy "no schema migration" refined —
  lookup-seed migration 0009 (probe_mode + 2 metric codes), no new
  tables; t6 sync updated to 41.
- **Error/rescue map:** 6 failure paths mapped (fetch, parse,
  clamp, CLI args, CSV write, AS-3 discovery) — 0 critical gaps
  after the amendments; failures land as run status + text finding,
  never silent.
- **Security:** no new deps/secrets; public GETs via the polite
  Fetcher; Low/Low Excel-formula note (CSVs are data) — no action.
- **Performance:** one GET, ~18k-row parse (<10 MB); p99 = network.
- **Rollback:** git revert; no migration/backfill; GO=1 gate.
- **Reversibility 5/5** (additive module, lookup-only migration).

## DECISIONS

- de1: probe-native command (not a script, not a run mode) — D7/D18.
- de2: no staging writes; samples are review artifacts (strategy
  Scope).
- de3: in-scope filter = category mechanism (group-044 / criterion
  096), reusing `_in_scope_group` — registers carry no CN8 codes.
- de4: expected-unavailable is exit 0; only should-deliver failures
  exit 2.

## OPEN ITEMS

- Nordic Swan export mechanics — bounded discovery attempt; browser
  pass fallback (carried in Strategy OPEN ITEMS). The trial
  discovery handler gets reconciled with the pilot unit's pinned
  mechanics (TODOS.md, v0.2.4 ENG review).
- Exact public ECAT export header — re-pinned by the first real run
  (lands in the manifest).

## Addendum — AS-class source probe (D37, 2026-09-14)

Built per ENG review (SMALL CHANGE, 2026-09-14; all recommendations
accepted — 1A sideways-import of csv_sample privates, 2A
copy-or-honest-unavailable, 3A evidence-first):

- Migration `0010__as_source_probe.sql` — lookup-only: probe_mode
  `as_source_probe` + 4 metric seeds (`as_probe_rows` numeric,
  `as_probe_records`/`as_probe_unavailable`/`as_probe_assoc` text).
- Module `src/leadhs/probe/as_probe.py` — `REGISTRY_IDS` (AS-2..AS-10)
  vs everything else (associations); `_registry` bounded discovery
  (≤5 GETs: pinned base + `/export.csv`, `/export.xlsx`, `/export`
  candidates, landing page included and reused for the estimate pass;
  CSV/TSV saved whole, XLSX rendered via xlsxmod; estimate regex on
  page text with basis + access date; failed pinned base → exit 2,
  later-candidate errors are discovery), `_association` (1 GET,
  member-count regex, 404/down = expected), `_reuse` (copy-if-exists
  of the newest same-day `csv-sample.*.AS-2/3.csv`), summary
  ALWAYS written (timestamped + latest, md+csv).
- CLI `leadhs probe as-source-probe` (`--out-dir`
  `data/report/AS-source-probe`, `--reuse-dir data/report`,
  `--source` multi, `--dry-run`); Makefile `as-probe` behind
  `guard-as-probe` (`|| test $? -eq 2`).
- Tests: `tests/test_as_probe.py` (18) incl. the register split
  guard against the real `dict/sources.csv` (30 AS rows = 9
  registries + 21 manual associations); conftest fixture paths
  `/as-export.xlsx`, `/as-count.html`, `/assoc.html`.
- Real run executed 2026-09-14 (see the strategy addendum);
  after the run, the candidate-URL join was normalized
  (`rstrip("/")`) to avoid double-slash request lines — today's
  artifacts stand as-is (they record what was actually requested).

No strategy change; reversibility additive (no table change).

## Addendum — parse correction, evidence archiving, data_sources.csv (D38, 2026-09-14)

Triggered by the v0.2.4 review (user + the raw-store inventory and
AS-probe report read-through of 2026-09-14). Three findings drive it;
the first was re-diagnosed twice and the final diagnosis is verified
by an offline reproduction against the archived export:

1. **The published AS-3 "14 distinct products" is wrong — twice
   over.** The archived Nordic Swan export
   (`data/raw/as-3/3ccc0aff….csv`, 9.8 MB, 52,544 lines) parsed
   CORRECTLY (semicolon-delimited; `_delimiter_of` picks `;`; the
   comma/quote wreckage visible in the sample rows is genuine source
   content, properly quoted by our writer). Two real defects produced
   the wrong numbers:
   - **Dedupe identity-triple column mismatch:** `_sample_delivered`
     looks up ECAT-style column names
     (`product_or_service_name`/`product_name`/`name`,
     `company_name`/`manufacturer`, `group_name`/`group`) — none exist
     in the Nordic Swan export (`product`, `licensee`,
     `product group`). Only `category` falls through, so the key was
     `("", "", category)` and the pool collapsed to the 14 distinct
     Category values — the "14 distinct product items" are 14
     categories, one arbitrary row each (verified: pool sim = 14).
   - **Loose scope filter:** `_in_scope_criterion096` matches
     substrings across the whole record — `"lack"` ⊂ "Black" (toner
     rows), `"paint"` ⊂ "painting" (cleaning-agent rows) — so
     out-of-scope rows flooded in and others were missed (2,283
     matched vs 2,424 true paint-group rows; the toner/chair/cleaning
     entries in the published sample are scope-filter failures, NOT
     parse artifacts).
   The true paint pool (group value ∈ {`EU44 Decorative paints,
   varnishes, and related products`, `096 Paints and varnishes`}):
   **2,424 rows / 2,323 distinct items / 53 licences / 20 licensees**
   (Nordic Swan 41 licences incl. 4 dual-listed, EU Ecolabel 16 incl.
   4 dual; 2,323 is distinct by the (product, licensee, licence)
   key — the sampling pool is 2,322 distinct by the implemented
   (product, licensee, product group) key). The export itself is
   clean; the mixed-scheme content (EU
   Ecolabel licences inside the Nordic Swan export) is the source's
   data model, joinable by licence number.
2. **Estimate evidence was not archived.** In as_probe `_registry`,
   the landing page is decoded in memory for the estimate pass but
   `store.put` only runs for CSV/XLSX candidates — the AS-4 ≈70,000
   estimate (register-claimed page text, all categories) has a URL +
   access date but no archived page. Violates the provenance rule for
   load-bearing numbers.
3. **Sample size vs register size was ambiguous** in the as-probe
   summary ("100 (exact)" next to a register of 17,013 distinct).

Design deltas (all offline, zero network):

- **csv_sample AS-3 fixes (two, both in code):**
  - scope filter switches from whole-record substrings to the
    structured group value (exact match on the `product group`
    column ∈ {EU44…, 096 Paints and varnishes}) — the same category
    mechanism as AS-2's group-044 (de3), now actually applied to the
    right column;
  - the dedupe identity triple becomes source-parameterized:
    AS-2 (product, company, group) via the ECAT columns, AS-3
    (product, licensee, product group) — the item identity the
    register actually exposes.
  Tests: fixture semicolon export with Black-laden out-of-scope rows
  (toner) + painting clean-agent rows excluded by the structured
  filter; pool counts; dedupe uses the AS-3 triple; no out-of-scope
  rows in the draw.
- **AS-3 re-render from the archived document** — fresh seeded draw
  n=100, seed=42, **same `raw_hash`** (the document is unchanged),
  new timestamped sample + regenerated manifest. Pool ≈2,423 distinct
  items → full n=100 draw (no clamp). No refetch. Published
  statements corrected accordingly (report-0.2.4, READMEs, management
  summary, TODOS: the "ragged-comma reconciliation" item closes —
  diagnosis corrected: the defects were filter + dedupe keys, not
  delimiter/quoting; the mixed-scheme note stays as a data-model
  fact).
- **as_probe evidence archiving:** when an estimate is derived from a
  landing page, `store.put` the page (ext `html`) and link the
  document into the finding — every estimate becomes re-checkable.
  Summary relabel: reuse rows render "100 rows (sample of 17,013
  distinct register products)". New offline `--rebuild-summary`
  re-renders `as-source-probe.summary.{md,csv}` from DB findings +
  artifact reads (no network) — needed because the AS-3 reuse
  artifact changes.
- **data_sources.csv** (new, `src/leadhs/dict/data_sources.csv`,
  alongside sources.csv; review artifact, NOT loaded by
  `source load`): lists only sources where a probe has **confirmed
  bulk data carrying the identity tuple** — bulk extract obtainable +
  manufacturer + product ident-nr present in that bulk data + in-scope
  (3208/3209) membership derivable from the source's category/group
  filter (registers carry no CN codes; HS-derivable, not a literal HS
  column). Initial content: **AS-2 and AS-3 only** (confirmed by our
  own runs; exports archived). Columns: `ds_id, source_id, name,
  operator, url, bulk_url, format, delimiter, rows_total,
  rows_in_scope, size_basis, size_date, identity_manufacturer,
  identity_ident, identity_notes, independence_group,
  verification_status, verified_date, listed_by, notes`. Header
  documents the gate + growth policy: rows enter only as future
  probes confirm; gated registers (KemI, Danish AT, PCN, KemiDigi,
  Norwegian P-no) and scrape-only directories stay OUT (they remain
  documented in DATA_SOURCE.md / strategy). Well-formedness test
  (header schema, ≥2 rows, no PE rows, provenance filled).
- **Next-unit input (strategy side):** the 2026-09-14 consultant
  handover (source-expansion + MSE methodology) is filed in
  `10_STRATEGY` as INPUT for the next strategy turn — candidate probe
  queue (KemiDigi, WINGIS/GefKomm-Bau, BASTA, INIES commercial
  references, eBVD, Quick-FDS, ECHA PT21; M1/EPD systems/Safety Gate/
  EBTI auxiliary-only), entry criterion = the confirmed-bulk gate
  above, dependency groups (ECAT+Nordic Swan same selection family;
  BASTA↔eBVD data exchange), bias strata for lead-prevalence
  sampling, MSE/manufacturer-census estimator proposals, exit
  criterion per handover §28. Corrections recorded: INIES webservice
  is licensed (our verified AS-probe finding), BASTA ">200,000
  articles" is a claim pending probe. Not adopted policy; no design
  or code rides on it in v0.2.4.

DECISIONS added:

- de5: data_sources.csv gate = confirmed bulk + identity tuple +
  HS-derivable scope; the file starts with AS-2/AS-3 and grows only
  on probe-confirmed evidence (user decision 2026-09-14).
- de6: gated registers and unprobed candidates never enter
  data_sources.csv on claims — they stay in DATA_SOURCE.md/strategy
  documentation (review finding; supersedes the D37-side "include
  gated" reading for this file).
- de7: the consultant MSE/source-expansion proposal is next-unit
  strategy input, not v0.2.4 scope (user decision 2026-09-14).
- de8: AS-3 sample re-rendered from the archived export (same
  raw_hash, new draw) — no network, full provenance retained.

OPEN ITEMS updated:

- RESOLVED (D38): the AS-3 "ragged-comma / trial-grade" caveats —
  final diagnosis: scope-filter + dedupe-key defects, parse was
  clean. The mixed-scheme export stays a documented data-model note
  (licence-number join).
- NEW (next unit): probe queue per the consultant handover, gated by
  de5 (filed in 10_STRATEGY; v0.2.4 carries no code for it).

## Addendum — full-probe round + source-expansion reconnaissance (D39, 2026-09-15)

Triggered by explicit user instruction ("run a probe run again over
all potential data sources with the new ones … with a new report
covering everything"). This executes the INPUT digest's PASS 1 probe
queue at reconnaissance level (the de7 deferral was about formal unit
adoption, not the probe itself). No new estimand methods adopted; no
strategy reversal — the reconnaissance frame (MASTER D31) covers it.

Design deltas:

- Register: six new AS rows (AS-31..AS-36) — KemiDigi (A+),
  WINGIS/GefKomm-Bau (A+), BASTA (A+), eBVD (A−), Quick-FDS (A−/B+),
  ECHA PT21 antifouling (B+); all `open` verification, active=1,
  provenance cites the handover + the INPUT digest. They are PROBE
  rows — entry into data_sources.csv stays behind the de5 gate.
- as_probe: REGISTRY_IDS extended to 15 ids (bounded export
  discovery covers the new six as registries); per-source
  what's-instead notes added; the summary header renders row count +
  registry count dynamically (was hardcoded "all 30").
- Split guard updated: 15 + 21 = 36 (intentional register change).
- 393→same suite green (register-content changes only; the CLI csv
  sample gains nothing — DEFAULT_SOURCES stays the six-registry D36
  target set; the new six are AS-probe territory).

Real runs (2026-09-15, all GO=1; report render 20260915-000613;
audit clean):

- census sweep: `GO=1 make census` (108 sources); the six new rows
  classify tier (c) (visible, uncounted without scraping); the
  blocked/failed site sets are unchanged (PE hosts + ST-2, as
  documented since v0.2.2/v0.2.3).
- csv-sample: fresh network run 20260915-000230 — AS-2/AS-3 delivered
  100 rows each; the corrected D38 pools reproduce exactly (17,013 /
  2,322 distinct) — the deterministic-draw claim verified on a
  second live download.
- as-source-probe: run 20260915-000255 over all 36 AS rows.
  Results: delivered AS-2/AS-3 (reuse), estimated AS-4 ≈70k / AS-7
  ≈2k, unavailable AS-5/6/8/9/10, 18 associations live + 3
  unreachable (all unchanged); NEW: AS-31..AS-35 unavailable -
  unknown (bounded discovery found no bulk export; honest
  what's-instead per source), AS-36 failed (ECHA robots.txt 403 -
  hardened host; failed-registry exit 2, tolerated).

New data-pool findings (D39 conclusion): **no third bulk source
today.** The confirmed-bulk set (data_sources.csv) stays DS-1 (AS-2)
+ DS-2 (AS-3); none of the six candidates exposed a bulk surface at
URL-guess level under the ≤5 polite GETs budget. Enumerability for
KemiDigi/BASTA/WINGIS/eBVD/Quick-FDS must be pinned per source (the
handover §19 deliverables: category explorations, API terms,
agreements) — that is next-unit work, not more guessing. The AS-36
ECHA host joins the known-hardened class (robots 403 — like the PE
hosts); a robots-compliant export pin (manual browser pass) is its
next step.

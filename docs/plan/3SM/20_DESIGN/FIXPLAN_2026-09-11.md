---
unit: v0.1.1
stage: DESIGN
lifecycle: LIVE
updated: 2026-09-11
---

# Design fix plan — critical + ENG review 2026-09-11

## Abstract

Consolidated work order from the design review sessions of 2026-09-11:
a critical review of the 20_DESIGN docs (P0 findings 1–6), an ENG
review in SMALL CHANGE mode (findings 1–4), and the user decisions
taken in both. This file is the executable plan for applying those
fixes to the Design docs and the two affected Strategy chart sources.
It is a provisional work order, not canonical structure: after
execution it is marked DONE (or deleted) — it does not change any
lifecycle state by itself (3SM process rule).

## Execution status (updated 2026-09-11)

- **C-1 APPLIED** — probe-process.md updated and re-rendered during
  the 2026-09-11 documentation update (the chart became README-facing
  and had to match the reviewed semantics).
- **C-2 MOOT** — run-state-machine chart archived 2026-09-11 by user
  decision (three of six charts moved to
  `10_STRATEGY/_archive/10_STRATEGY/charts/`); the authoritative run
  states remain in the written Design docs. Sections VI/C-3 below now
  cover only probe-process.
- **I / D / A / T / M items: NOT YET EXECUTED** — still pending
  explicit instruction; the edit strings below remain valid.

## Decision record (mandate)

- **Blocked/done rule (critical #1):** OUTCOME-BASED — reachable with
  findings collected → `done` (exit 0); wholly inaccessible (robots
  deny / 403 / persistent 429 / paywall) → `blocked` (exit 2);
  unrecoverable network error → `failed` (exit 2). Findings collected
  before a block keep the run `done` + notes.
- **Scope (critical review):** apply P0 fixes 1–6 only; P1/P2 are
  left open and routed into OPEN ITEMS (3SM equivalent of TODOs —
  repo has no TODOS.md by convention).
- **ENG review mode:** SMALL CHANGE; findings accepted:
  **1A** stale-run reclaim, **2A** `RateLimited` split from `Blocked`,
  **3A** backup-restore + stale-run tests. Perf: no critical issue;
  two notes (covering index, pacing line).
- Not decided / out of this plan: P1 #7 (CS-1 dual mode — already an
  open item), P1 #8 resolution (only recorded as open), M1–M4
  implementation.

## Files touched

    docs/plan/3SM/20_DESIGN/MASTER/interfaces.md      (I-1..I-7)
    docs/plan/3SM/20_DESIGN/MASTER/data_model.md      (D-1..D-11)
    docs/plan/3SM/20_DESIGN/MASTER/architecture.md    (A-1..A-7)
    docs/plan/3SM/20_DESIGN/MASTER/testing.md         (T-1..T-6)
    docs/plan/3SM/20_DESIGN/MASTER.md                 (M-1; see note)
    docs/plan/3SM/10_STRATEGY/charts/probe-process.md (C-1) + re-render
    docs/plan/3SM/10_STRATEGY/charts/run-state-machine.md (C-2) + re-render

`units/v0.1.1.md` is NOT touched (its references stay valid; exit-code
convention 0/1/2/3 unchanged). `charts/README.md` needs no change
(sources of truth unchanged).

**M-1 note:** the earlier approved plan said "MASTER.md: no edits".
Deviation justified: new detail codes (a11, i8, i9, dm14, dm15, t6)
added in topic docs must appear in MASTER's d-line code references or
the dashboard mapping dangles; also the Readiness line states the ENG
review is pending, which is now false. No new d-entries, only
parenthetical code-list extensions + the Readiness sentence.

---

## I. interfaces.md

### I-1 Exception table — RobotsDisallowed row

OLD:
```
| `RobotsDisallowed` | robots.txt disallows our paths | finding robots_denied (method=manual) + run done |
```
NEW:
```
| `RobotsDisallowed` | robots.txt disallows our paths | finding robots_denied (method=manual) + run blocked (exit 2); findings already collected → run done + notes |
```

### I-2 Exception table — Blocked row + new RateLimited row (2A)

OLD:
```
| `Blocked` | HTTP 403/429 or paywall/login wall | finding access_blocked + run done |
```
NEW (two rows):
```
| `Blocked` | HTTP 403 or paywall/login wall | finding access_blocked + run blocked (exit 2); findings already collected → run done + notes |
| `RateLimited` | HTTP 429 | one capped backoff then retry; persistent 429 → finding access_blocked + run blocked (exit 2) |
```

### I-3 Run semantics — update Abort/resume parenthetical + add three bullets

In the **Abort/resume** bullet, OLD `(latest run per source × metric)`
→ NEW `(latest done run per source × metric)`.

After that bullet, ADD:
```
- **Outcome rule:** run status reflects the whole source — reachable
  with findings collected → `done`; wholly inaccessible (robots deny,
  403, persistent 429, paywall) → `blocked`; unrecoverable network
  error → `failed`. `probe run` exits 2 when any run ended blocked or
  failed; findings collected before a block keep the run `done` with
  a notes line.
- **Stale-run reclaim:** a run left `running` by a hard crash
  (SIGKILL/power loss) is marked `failed` with note
  `stale run reclaimed` by the next `db init` / `probe run` /
  `db status` (threshold: started_at older than the current process,
  or 1 h); the census is unaffected — v_probe_latest reads `done`
  runs only.
- **Pacing:** `probe run --all` on the full register is a multi-minute
  operation (≥ 2 s spacing per domain); progress via `--verbose`;
  re-runs are incremental (per-source new runs).
```

### I-4 Metrics extension rule

OLD:
```
Extension rule: new metrics are added by a migration INSERT (code
never renamed; value_type fixed at insert). `metrics.py` is the single
source of truth shared by migration 0001 and runtime validation.
```
NEW:
```
Extension rule: new metrics are added by a migration INSERT (code
never renamed; value_type fixed at insert). `metrics.py` is the
runtime source of truth; a sync test pins migration 0001's seeds to
it (0001 is static SQL and cannot call Python).
```

### I-5 DECISIONS — add

```
- i8: `RateLimited` (429) split from `Blocked` (403/paywall): one
  capped backoff, then blocked (ENG review 2026-09-11).
- i9: outcome-based run status — done / blocked / failed per source;
  exit 2 on any blocked or failed run (ENG review 2026-09-11).
```

### I-6 OPEN ITEMS — add (P1/P2 routing)

```
- `probe record` = one run per finding — revisit batching (one manual
  run, multiple findings) at M1 if PCN passes produce run churn.
- `sample_n`/`mode` stored in both `parameters_json` and typed columns
  (`probe_run.mode_code`) — pick one home at implementation.
```

### I-7 Frontmatter

`updated: 2026-09-10` → `updated: 2026-09-11`.

### Verified — no edit needed

- Exit-code table row `probe run` ("0 all done; 2 any run
  failed/blocked") is already correct under the outcome rule.
- `KeyboardInterrupt` row (run aborted; exit 130) unaffected.

---

## II. data_model.md

### D-1 Abstract (portability claim)

OLD:
```
The schema is written to port to PostgreSQL unchanged and to be
reusable for future substance-in-products studies via a `study`
discriminator.
```
NEW:
```
The schema is written to port to PostgreSQL with two documented
variances (id generation, one partial index) and to be reusable for
future substance-in-products studies via a `study` discriminator.
```

### D-2 Design principle 8

OLD:
```
8. **Portability** — SQLite now (WAL, foreign keys ON); DDL avoids
   SQLite-only constructs so it ports to PostgreSQL unchanged.
```
NEW:
```
8. **Portability** — SQLite now (WAL, foreign keys ON); the DDL stays
   standard SQL except two documented variances (rowid-alias ids, one
   partial index), listed in the portability section.
```

### D-3 Conventions — entity id line

OLD:
```
- Entity tables: `id INTEGER PRIMARY KEY` (rowid; no AUTOINCREMENT).
```
NEW:
```
- Entity tables: `id INTEGER PRIMARY KEY` (rowid alias; no
  AUTOINCREMENT — PostgreSQL port swaps to IDENTITY; variance).
```

### D-4 run table — status default (critical #2)

OLD:
```
- status_code → run_status NOT NULL DEFAULT 'running'
```
NEW:
```
- status_code → run_status NOT NULL DEFAULT 'planned' (initial state
  per the run lifecycle; the engine moves it to running/done)
```

### D-5 run table — indexes (perf note a)

OLD:
```
- Index: (source_id), (kind_code), (status_code).
```
NEW:
```
- Index: (source_id), (kind_code), (status_code),
  (source_id, status_code, started_at) — covering v_probe_latest.
```

### D-6 document table — add run_id (critical #4)

After the line `- id INTEGER PK; source_id → source NOT NULL`, INSERT:
```
- run_id → run NOT NULL (retrieved inside a run — P1/R9 hold for
  documents by construction)
```
And the index line OLD `- Index: (source_id); UNIQUE (raw_hash).` NEW:
```
- Index: (source_id), (run_id); UNIQUE (raw_hash).
```

### D-7 v_probe_latest — done filter (critical #3)

OLD:
```
- **v_probe_latest** — the current finding per (source, metric):
  latest run (by started_at, then run id) per source × metric; the
  "current census" under re-runs.
```
NEW:
```
- **v_probe_latest** — the current finding per (source, metric):
  latest **done** run (status_code='done'; by started_at, then run
  id) per source × metric; aborted/failed/blocked runs keep their
  findings but never shadow the current census.
```

### D-8 product_classification partial index — variance note

OLD:
```
- Partial UNIQUE index: (product_id) WHERE active = 1 — one current
  classification, full history retained.
```
NEW:
```
- Partial UNIQUE index: (product_id) WHERE active = 1 — one current
  classification, full history retained (partial-index syntax is the
  second PostgreSQL variance).
```

### D-9 Portability section

OLD:
```
- **PostgreSQL port:** type variances only (BOOLEAN, JSONB); no
  SQLite-only constructs in migrations (no AUTOINCREMENT, no PRAGMA —
  pragmas are runtime connection settings in db.py).
```
NEW:
```
- **PostgreSQL port:** documented variances only — BOOLEAN, JSONB, id
  generation (`INTEGER PRIMARY KEY` rowid alias → IDENTITY/BIGSERIAL),
  and the `product_classification` partial-index syntax; no other
  SQLite-only constructs in migrations (no AUTOINCREMENT, no PRAGMA —
  pragmas are runtime connection settings in db.py).
```

### D-10 DECISIONS — add

```
- dm14: run lifecycle in schema — initial status `planned`;
  v_probe_latest restricted to `done` runs (ENG review 2026-09-11).
- dm15: document.run_id NOT NULL — the provenance backbone is
  run-complete; P1/R9 hold for documents by construction.
```

### D-11 OPEN ITEMS — add (P2 routing)

```
- Backup target directory — propose `data/backups/` (gitignored);
  confirm at implementation.
- trade_stat.run_id nullable vs P1 — confirm import-only provenance
  (manual trade-stat entry would need a run or an exception rule).
```

### D-12 Frontmatter

`updated: 2026-09-10` → `updated: 2026-09-11`.

---

## III. architecture.md

### A-1 fetch.py contract — exception taxonomy (2A)

OLD:
```
  env or flag — doctor warns when unset); 2 retries with exponential
  backoff; `RobotsDisallowed` and `Blocked` (403/429) raised as typed
  errors.
```
NEW:
```
  env or flag — doctor warns when unset); 2 retries with exponential
  backoff; `RobotsDisallowed`, `Blocked` (403/paywall) and
  `RateLimited` (429 — one capped backoff, then Blocked) raised as
  typed errors.
```

### A-2 Package layout comment — metrics.py

OLD:
```
       metrics.py         # probe-metric constants + seed list
                          # (single source of truth for 0001 + code)
```
NEW:
```
       metrics.py         # probe-metric constants + seed list
                          # (runtime source of truth; 0001 seeds
                          # pinned to it by a sync test)
```

### A-3 Module contracts — metrics.py

OLD:
```
- **metrics.py** — `METRIC_*` constants + `PROBE_METRIC_SEEDS`;
  migration 0001 and runtime validation both use this list.
```
NEW:
```
- **metrics.py** — `METRIC_*` constants + `PROBE_METRIC_SEEDS`;
  runtime validation uses this list; a sync test asserts migration
  0001's seeds match it exactly.
```

### A-4 Failure behavior — add stale-run reclaim bullet (1A)

After the "Per-domain backoff and retry in fetch.py; sustained failure
→ blocked finding + continue." bullet, ADD:
```
- Stale-run reclaim: a run left `running` by a hard crash (SIGKILL/
  power loss) is marked `failed` with note `stale run reclaimed` by
  the next `db init` / `probe run` / `db status`; the census is
  unaffected (v_probe_latest reads done runs only).
```

### A-5 Probe data-flow diagram — replace whole ASCII block

Replace the block under `### Probe data flow — happy, empty, error
paths` with:
```
             ┌───────────────────────────┐
             │ probe run --source PE-1   │
             └────────────┬──────────────┘
                           │
         ┌─────────────────▼─────────────────┐
         │ load source + robots/terms/rate   │
         └──────┬──────────────┬─────────────┘
     happy      │              │ error
       ▼        ▼              ▼
   enumerate  robots deny   fetch fail
   categories   │            (retry 2x,
       │        ▼             backoff;
       ▼     finding         429: one capped
   fetch pages (robots_     backoff first)
   ≤1 req/2s  denied,         ▼
       │      manual);      finding(access_
       │      run=blocked,  blocked, manual)
       │      exit 2          │
       │                      ▼
       │                  run=blocked,
       │                  exit 2 (findings
       │                  collected before
       │                  → run=done+notes)
       ▼
   document (raw) + findings (catalog_count,
   category_count, sds_sample_ok, languages,
   terms, rate_limit)
       ▼
   run → done
   empty path: no active sources → "no sources", exit 0
```

### A-6 Run state machine ASCII — append two lines

OLD (last two lines of the block):
```
   blocked: manual fallback recorded as finding
   aborted: interrupt; inserted findings persist
```
NEW (append):
```
   blocked: manual fallback recorded as finding
   aborted: interrupt; inserted findings persist
   stale running (SIGKILL/power loss) → failed
   ("stale run reclaimed") on next db/probe command
```

### A-7 DECISIONS — add

```
- a11: stale-run reclaim — hard-crash `running` rows are marked
  failed with a `stale run reclaimed` note by the next db/probe
  command (ENG review 2026-09-11).
```

### A-8 Frontmatter

`updated: 2026-09-10` → `updated: 2026-09-11`.

---

## IV. testing.md

### T-1 test_migrations.py — append coverage (3A + sync test)

OLD:
```
- **test_migrations.py** — fresh DB applies 0001→0002; re-run is a
  no-op (idempotency via schema_version); migration order enforced;
  CHECK constraints hold (per-source probe run; archived ⇒ hash).
```
NEW:
```
- **test_migrations.py** — fresh DB applies 0001→0002; re-run is a
  no-op (idempotency via schema_version); migration order enforced;
  CHECK constraints hold (per-source probe run; archived ⇒ hash);
  backup round-trip (seeded DB → migrate → timestamped backup
  restores a working DB); sync test (0001 probe_metric seeds ==
  metrics.py list).
```

### T-2 test_fetch.py — 429 split (2A)

OLD:
```
- **test_fetch.py** — fake clock: ≥2 s spacing per domain; robots
  deny → RobotsDisallowed; 403/429 → Blocked; retry 2× then
  ProbeNetworkError; dry-run mode performs zero network calls.
```
NEW:
```
- **test_fetch.py** — fake clock: ≥2 s spacing per domain; robots
  deny → RobotsDisallowed; 403 → Blocked; 429 → RateLimited (one
  capped backoff) → retry succeeds, persistent 429 → blocked; retry
  2× then ProbeNetworkError; dry-run mode performs zero network
  calls.
```

### T-3 test_engine.py — stale-run reclaim test (1A)

OLD:
```
- **test_engine.py** — one run per source; `--all` continues past a
  failing source; interrupt → run aborted, findings persist; re-run
  → new run_key (-2 suffix); exit codes.
```
NEW:
```
- **test_engine.py** — one run per source; `--all` continues past a
  failing source; interrupt → run aborted, findings persist; re-run
  → new run_key (-2 suffix); exit codes; stale-run reclaim
  (pre-inserted stale `running` run → next command marks it failed
  with `stale run reclaimed`).
```

### T-4 test_views.py — done-filter case (critical #3)

OLD:
```
- **test_views.py** — v_probe_latest correctness under re-run (newer
  run wins); v_anchor_candidates lists count-metrics; v_source_activity
  first/last retrieval.
```
NEW:
```
- **test_views.py** — v_probe_latest correctness under re-run (newer
  done run wins); a later aborted/blocked run does not shadow the
  last done census; v_anchor_candidates lists count-metrics;
  v_source_activity first/last retrieval.
```

### T-5 Chaos test — add assertion

OLD:
```
Kill a probe run mid-source (simulated interrupt): run=aborted,
findings persisted, re-run idempotent, `db audit` still clean.
```
NEW:
```
Kill a probe run mid-source (simulated interrupt): run=aborted,
findings persisted, re-run idempotent, `db audit` still clean,
v_probe_latest unchanged by the aborted run.
```

### T-6 DECISIONS — add

```
- t6: crash/backup coverage — metrics sync, backup round-trip,
  stale-run reclaim, 429-retry tests (ENG review 2026-09-11).
```

### T-7 Frontmatter

`updated: 2026-09-10` → `updated: 2026-09-11`.

---

## V. 20_DESIGN/MASTER.md (M-1)

### M-1a DECISIONS — extend code references only (no new d-entries)

- d2: `(dm2, a-series)` → `(dm2, dm15)`
- d7: `(a2, ud1)` → `(a2, ud1, a11, dm14, i9)`
- d9: `(i4, ud7)` → `(i4, ud7, t6)`
- d10: `(a1, a3, a4, i3)` → `(a1, a3, a4, i3, i8)`
- d20: `(a8, ud6)` → `(a8, ud6, t6)`

### M-1b Readiness line

OLD:
```
- **v0.1.1: ready.** Design converged (schema fixed to column level,
  contracts and test matrix written). Next step per 3SM: engineering
  review of this design (optional), then 30_IMPLEMENTATION/v0.1.1/
  PHASE##.md when explicitly instructed.
```
NEW:
```
- **v0.1.1: ready.** Design converged (schema fixed to column level,
  contracts and test matrix written); engineering review done
  2026-09-11 (critical + ENG review; findings applied per
  FIXPLAN_2026-09-11.md). Next step per 3SM:
  30_IMPLEMENTATION/v0.1.1/PHASE##.md when explicitly instructed.
```

### M-1c Frontmatter

`updated: 2026-09-10` → `updated: 2026-09-11`.

---

## VI. Charts (C)

### C-1 probe-process.md — outcome-based semantics

Exact edits (file has no frontmatter; keep header lines 1–5):
- Step 3: `run done` → `run blocked, exit 2 (findings already
  collected → run done + notes)`
- Step 4: append `; 429 → one capped backoff (RateLimited), then
  blocked`
- Step 5: `HTTP 403/429 or paywall/login?` → `HTTP 403, persistent
  429, or paywall/login?`; its yes-branch `run done` → `run blocked,
  exit 2 (findings already collected → run done + notes)`
- ASCII fallback, robots-deny line: `run done` → `run blocked, exit 2`
- ASCII fallback, fetch-fail line: `finding(blocked, manual); run
  done (partial)` → `finding(access_blocked, manual); run blocked,
  exit 2 (partial → done+notes)`

### C-2 run-state-machine.md — blocked semantics + reclaim edge

- State `done`: `findings + documents written (access blocks recorded
  as findings + notes)` → `findings + documents written (a block
  after partial collection → done + notes)`
- State `blocked`: `source wholly inaccessible` → `source wholly
  inaccessible (robots deny / 403 / persistent 429 / paywall)`
- Transitions: ADD
  `- running → failed on stale-run reclaim (SIGKILL/power loss; next
  db/probe command marks it failed, note "stale run reclaimed")`
- ASCII fallback: append under `failed/blocked/aborted`:
  `stale running → failed ("stale run reclaimed") on next command`

### C-3 Re-render both charts

From the repo root:
```
export ZAI_API_KEY=$(python3 -c "import json;print(json.load(open('/home/fsncps/.local/share/opencode/auth.json'))['zai-coding-plan']['key'])")
python3 /tmp/opencode/render_chart.py docs/plan/3SM/10_STRATEGY/charts/probe-process.md flowchart
python3 /tmp/opencode/render_chart.py docs/plan/3SM/10_STRATEGY/charts/run-state-machine.md state_machine
```

If `/tmp/opencode/render_chart.py` is gone (tmp cleanup), recreate
with this exact content:
```python
#!/usr/bin/env python3
"""Render a flowchart chart via the flowchart skill with the Z.ai
coding-plan endpoint patched in at runtime (skill files untouched).

Usage: render_chart.py <input.md> <chart-type>
Requires ZAI_API_KEY in the environment.
"""
import sys
import asyncio

SKILL = "/home/fsncps/.config/opencode/skills/flowchart"
sys.path.insert(0, SKILL)

from flowchart.providers.zai import ZaiProvider  # noqa: E402

ZaiProvider.API_BASE = "https://api.z.ai/api/coding/paas/v4"

from flowchart.__main__ import generate_flowchart  # noqa: E402

path, ctype = sys.argv[1], sys.argv[2]
sys.exit(asyncio.run(generate_flowchart(input_path=path, chart_type=ctype)))
```

Notes: the skill's own `providers/zai.py` hardcodes a wrong API_BASE
(`https://api.z.ai/v1` → 404); the patch above is the known-good
route for the coding-plan key. The user's `~/.secrets/API_zai.env`
key (via `.envrc`) is currently 401-rejected (likely truncated) —
use the `zai-coding-plan` key from opencode's auth store as shown.

---

## Execution order

1. interfaces.md (I-1..I-7)
2. data_model.md (D-1..D-12)
3. architecture.md (A-1..A-8)
4. testing.md (T-1..T-7)
5. 20_DESIGN/MASTER.md (M-1)
6. Chart sources (C-1, C-2), then re-render (C-3)
7. Verification (below)
8. Report, mark this plan DONE at the top, HALT.

Execution principles: apply OLD→NEW strings exactly; if a target
string no longer matches (docs drifted), re-read the section and
adapt the same semantic change — never skip silently; no commit, no
push, no LOG entry (routine edits per 3SM process), no lifecycle
changes.

## Verification checklist

```
rg -n "^updated:" docs/plan/3SM/20_DESIGN/ -g '*.md'
# expect 2026-09-11 on MASTER.md + 4 topic docs; units/v0.1.1.md stays 2026-09-10

rg -n "run done" docs/plan/3SM/20_DESIGN/MASTER/interfaces.md
# expect no matches in the exception table (KeyboardInterrupt row reads 'run aborted')

rg -n "DEFAULT 'planned'" docs/plan/3SM/20_DESIGN/MASTER/data_model.md          # 1 hit
rg -n "run_id → run NOT NULL" docs/plan/3SM/20_DESIGN/MASTER/data_model.md      # 1 hit
rg -n "latest \*\*done\*\* run" docs/plan/3SM/20_DESIGN/MASTER/data_model.md    # 1 hit
rg -n "a11|dm14|dm15|i8|i9|t6" docs/plan/3SM/20_DESIGN/ -g '*.md'
# codes present in topic docs AND referenced in MASTER.md

ls -la docs/plan/3SM/10_STRATEGY/charts/
# probe-process.* and run-state-machine.* freshly rendered; png/svg/dot non-zero
```
Then text-check the two new `.dot` files: probe-process.dot must end
the robots/blocked paths at a blocked terminal (not the done
terminal); run-state-machine.dot must contain the dashed
stale-run-reclaim transition.

## NOT in scope (explicitly deferred)

- P1 #7 CS-1 dual `format_check+census` mode — stays open
  (units/v0.1.1.md OPEN ITEMS).
- P1 #8 `probe record` batching — recorded as open (I-6), not
  resolved.
- PE seed-list sequencing (P4 13 rows vs P8 enumeration) — already
  open in units/v0.1.1.md.
- M1–M4 implementation, strategy freeze, root-MASTER stage update,
  LOG entry, AGENTS/README pointers — all await explicit instruction.
- Parquet, PostgreSQL deployment, second-study data.
